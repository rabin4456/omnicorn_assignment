export const enum ButtonType {
  PRIMARY = "primary",
  SECONDARY = "secondary",
}

export const enum AccordionType {
  PRIMARY = "primary",
  SECONDARY = "secondary",
}

export const enum HeaderType {
  SMALL = "sm",
  MEDIUM = "md",
  LARGE = "lg",
}
